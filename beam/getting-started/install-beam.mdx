---
title: "Install Beam"
---

<Tip>This should take less than 5 minutes.</Tip>

1. [Create a Slai account](https://www.slai.io/login) and get your API keys from the
   [dashboard](https://www.slai.io/beam/apps/settings/api-keys).

2. In a terminal, install the Beam CLI:

```bash
curl https://raw.githubusercontent.com/slai-labs/get-beam/main/get-beam.sh -sSfL | sh
```

3. Run `beam configure` \- you'll be prompted to enter your API keys set up a
   profile with API keys.

4. Set up a new folder wherever you want and create a virtual env:
   `python3 -m virtualenv .venv`

5. Source virtual env: `source .venv/bin/activate`

6. Install Beam SDK: `pip install beam-sdk`

Now you're ready to start developing!

# Get started with an example

<Card
  title="Stable Diffusion on GPU"
  icon="layer-group"
  href="beam/getting-started/stable-diffusion-gpu"
>
  Deploy Stable Diffusion using a GPU
</Card>

<Card
  title="Web Scraping"
  icon="ghost"
  href="beam/getting-started/running-a-web-scraper"
>
  Scraping a website and running the results through an ML model
</Card>

<Card
  title="Huggingface REST API"
  icon="bolt"
  href="beam/getting-started/deploying-an-ml-model"
>
  Deploying a pre-trained ML model as a REST API
</Card>
