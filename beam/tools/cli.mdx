---
title: "CLI"
description: "Build and deploy Beam apps directly from the command line"
---

### CLI Usage

| Command                  | Description                                                                   |
| ------------------------ | ----------------------------------------------------------------------------- |
| `configure`              | Configures your Beam API keys and saves a profile to `~/.beam/config`         |
| `start <your app name>`  | Starts a remote runtime based on the spec defined in your `beam.app()` config |
| `deploy <your app name>` | Deploys your app using whatever trigger is defined in the `beam.app()`        |
